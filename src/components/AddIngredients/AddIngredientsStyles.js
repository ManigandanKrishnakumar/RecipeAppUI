import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
  },
  heading: {
    marginStart: 0,
  },
  searchBox: {
    marginTop: 25,
  },
});

export default styles;
